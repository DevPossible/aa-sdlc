# Plugins

Optional packs that add skills, commands, or workflow steps without changing core. Two kinds:
tech-stack packs (for example `dotnet`, `node`) and process packs (for example a compliance
review). Plugins never wrap the ticket system, wiki, or source control.
