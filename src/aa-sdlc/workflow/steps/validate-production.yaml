id: validate-production
name: Validate production
discipline: operations
command: /aa-ops-validate-production
summary: >
  After a release, confirm from production evidence that the system is doing what the release
  claimed: run the production validation checks, compare metrics to the baseline, and report.
anchor: required
inputs:
  - The release, its verification checks, and its notes
  - Dashboards, metrics, and logs since the release
  - The performance baseline
methodology:
  phase: 5
  steps: ["5.3"]
artifacts:
  - name: Production validation results
    location: the release ticket
    acceptance:
      - Every check has a result with evidence
  - name: Production metrics report
    location: the knowledge base
    acceptance:
      - Compares key metrics before and after, computed with a tool, with any regression ticketed
guidance: [G-02, G-04, G-15, G-22, G-24, G-26]
guidance_inline:
  - Compare, do not glance. Compute before-and-after for each metric over a stated window; a dashboard that "looks fine" is not evidence.
  - Validation is read-only. Never change production to make a check pass; a failing check is an incident or a rollback.
requires: [R-02, R-03, R-04]
tenets: [T-07, T-10]
