# Commands

Target-neutral command definitions. Each names the skill it loads and the arguments it accepts,
and nothing more. The installer renders these into each target's command format using the
templates in `../targets/`.
